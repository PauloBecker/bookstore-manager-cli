
export function formatMensagemSucesso(mensagem: string): string {
  return `✅ Sucesso: ${mensagem}`;
}

export function formatMensagemErro(mensagem: string): string {
  return `❌ Erro: ${mensagem}`;
}

